/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    // images domain
    images: {
        domains: ['res.cloudinary.com'],
    },
}

module.exports = nextConfig
